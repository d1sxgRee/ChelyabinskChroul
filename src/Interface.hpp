#ifndef __INTERFACE_H__
#define __INTERFACE_H__

void drawInterface(const Hero& hero, HDC hp_sprite);

void drawInterface(const Hero& hero, HDC hp_sprite, )
{
    int hp = hero.getData().health_points;
    long int
}
#endif //__INTERFACE_H__
